Flask==2.3.3  # Corrected from 2.3.4, the latest 2.x version
joblib==1.4.2
matplotlib==3.10.1
numpy==2.2.4  # This version is valid, but consider using a less strict specifier like `numpy>=2.2.0`
pandas==2.2.3
pdfkit==1.0.0
plotly==5.24.1
protobuf>=4.23,<5  # This range is fine, but you could try `>=4.23`
requests==2.32.3
seaborn==0.13.2
gunicorn==21.2.0 # **CRITICAL** This version has security vulnerabilities; it is strongly recommended to upgrade to a newer version like `gunicorn>=22.0.0` or `gunicorn>=23.0.0`.
google-generativeai==0.8.5